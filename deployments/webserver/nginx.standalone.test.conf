# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.standalone.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/canvas/webserver.pid;
error_log /var/log/sdkwork/canvas/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    include snippets/adaptive-web.maps.conf;
    upstream gateway {
        least_conn;
        keepalive 32;
        server 127.0.0.1:3900;
    }
    server {
        listen 80;
        server_name canvas-test.sdkwork.com canvas-test.birdcoder.com canvas-test.dtupay.com canvas-test.sdkwork.cn canvas-test.birdcoder.cn canvas-test.dtupay.cn canvas-test.skubc.com canvas-test.skubc.cn canvas-test.zowalk.com canvas-test.zowalk.cn canvas-test.offer86.com canvas-test.offer86.cn canvas-test.86offer.com canvas-test.86offer.cn;
        include snippets/gateway-locations.nonproduction.conf;
    }
}
